import Foundation
import UIKit

extension UIStoryboard {
    enum Storyboard: String {
        case Main
        case Authentication
        case PopUp
        case TabControllers
        case Profile
    }
}
